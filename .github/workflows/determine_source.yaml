name: Determine event source

on:
  push:
  workflow_dispatch:
  release:
    published:

jobs:
  where-did-you-come-from:
    runs-on: ubuntu-latest
    steps:
      - name: Determine source
        run: |
          echo "Event name: ${{ github.event_name }}"
          echo "Event Head Ref: ${{ github.event.head_ref }}"
          echo "Event Ref: ${{ github.event.ref }}"
          echo "Event Ref Type: ${{ github.event.ref_type }}"
          echo "Event Ref Name: ${{ github.event.ref_name }}"
  where-did-you-go:
    runs-on: ubuntu-latest
    steps:
      - name: Whole dang thing
        run: |
          echo '${{ toJson(github) }}'

          